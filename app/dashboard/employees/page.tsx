export default function EmployeesPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-4">Employees</h1>
      <p className="text-muted-foreground">Admin's first real task — invite/create employees and managers, assign roles, set manager relationships. Nothing else works until people exist.</p>
    </div>
  );
}
