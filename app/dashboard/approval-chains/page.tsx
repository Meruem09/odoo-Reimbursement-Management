export default function ApprovalChainsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-4">Approval Chains</h1>
      <p className="text-muted-foreground">Define the chains (steps + order + who approves). Second setup task — expenses can't be submitted meaningfully without a chain.</p>
    </div>
  );
}
